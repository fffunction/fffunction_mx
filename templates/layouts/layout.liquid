<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>{{ global.project }}</title>
<link rel="shortcut icon" type="image/ico" href="/favicon.ico" />

{% include 'libs/gridset' %}
{% include 'libs/foundation_css' %}
{{ "screen.css" | asset_url | stylesheet_tag }}

{% include 'libs/headScript' %}

</head>
    <body class="">
        <div class="wrapper">
            {% block content %}
            {% endblock %}
        </div>
    </body>
    {{ global.network }}
    {% if global.network == 'true' %}
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js"></script>
    {% else %}
        {{ "jquery-1.8.2.min.js" | asset_url | script_tag }}
    {% endif %}

    {% include 'libs/foundation_js' %}
    {% include 'libs/javascriptLibs' %}
    {{ "script.js" | asset_url | script_tag }}
</html>
